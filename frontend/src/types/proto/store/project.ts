/* eslint-disable */
import Long from "long";
import _m0 from "protobufjs/minimal";

export const protobufPackage = "bytebase.store";

export interface Label {
  value: string;
  color: string;
  group: string;
}

export interface Project {
  issueLabels: Label[];
  /** Force issue labels to be used when creating an issue. */
  forceIssueLabels: boolean;
  /** Allow modifying statement after issue is created. */
  allowModifyStatement: boolean;
  /** Enable auto resolve issue. */
  autoResolveIssue: boolean;
  /** Enforce issue title created by user instead of generated by Bytebase. */
  enforceIssueTitle: boolean;
  defaultBackupBehavior: Project_DefaultBackupBehavior;
}

export enum Project_DefaultBackupBehavior {
  DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED = "DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED",
  DEFAULT_BACKUP_BEHAVIOR_NO_BACKUP = "DEFAULT_BACKUP_BEHAVIOR_NO_BACKUP",
  DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_STOP = "DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_STOP",
  DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_SKIP = "DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_SKIP",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function project_DefaultBackupBehaviorFromJSON(object: any): Project_DefaultBackupBehavior {
  switch (object) {
    case 0:
    case "DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED":
      return Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED;
    case 1:
    case "DEFAULT_BACKUP_BEHAVIOR_NO_BACKUP":
      return Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_NO_BACKUP;
    case 2:
    case "DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_STOP":
      return Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_STOP;
    case 3:
    case "DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_SKIP":
      return Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_SKIP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Project_DefaultBackupBehavior.UNRECOGNIZED;
  }
}

export function project_DefaultBackupBehaviorToJSON(object: Project_DefaultBackupBehavior): string {
  switch (object) {
    case Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED:
      return "DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED";
    case Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_NO_BACKUP:
      return "DEFAULT_BACKUP_BEHAVIOR_NO_BACKUP";
    case Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_STOP:
      return "DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_STOP";
    case Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_SKIP:
      return "DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_SKIP";
    case Project_DefaultBackupBehavior.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function project_DefaultBackupBehaviorToNumber(object: Project_DefaultBackupBehavior): number {
  switch (object) {
    case Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED:
      return 0;
    case Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_NO_BACKUP:
      return 1;
    case Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_STOP:
      return 2;
    case Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_BACKUP_ON_ERROR_SKIP:
      return 3;
    case Project_DefaultBackupBehavior.UNRECOGNIZED:
    default:
      return -1;
  }
}

function createBaseLabel(): Label {
  return { value: "", color: "", group: "" };
}

export const Label = {
  encode(message: Label, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    if (message.value !== "") {
      writer.uint32(10).string(message.value);
    }
    if (message.color !== "") {
      writer.uint32(18).string(message.color);
    }
    if (message.group !== "") {
      writer.uint32(26).string(message.group);
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Label {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLabel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.value = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.color = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.group = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Label {
    return {
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      color: isSet(object.color) ? globalThis.String(object.color) : "",
      group: isSet(object.group) ? globalThis.String(object.group) : "",
    };
  },

  toJSON(message: Label): unknown {
    const obj: any = {};
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.color !== "") {
      obj.color = message.color;
    }
    if (message.group !== "") {
      obj.group = message.group;
    }
    return obj;
  },

  create(base?: DeepPartial<Label>): Label {
    return Label.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Label>): Label {
    const message = createBaseLabel();
    message.value = object.value ?? "";
    message.color = object.color ?? "";
    message.group = object.group ?? "";
    return message;
  },
};

function createBaseProject(): Project {
  return {
    issueLabels: [],
    forceIssueLabels: false,
    allowModifyStatement: false,
    autoResolveIssue: false,
    enforceIssueTitle: false,
    defaultBackupBehavior: Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED,
  };
}

export const Project = {
  encode(message: Project, writer: _m0.Writer = _m0.Writer.create()): _m0.Writer {
    for (const v of message.issueLabels) {
      Label.encode(v!, writer.uint32(18).fork()).ldelim();
    }
    if (message.forceIssueLabels === true) {
      writer.uint32(24).bool(message.forceIssueLabels);
    }
    if (message.allowModifyStatement === true) {
      writer.uint32(32).bool(message.allowModifyStatement);
    }
    if (message.autoResolveIssue === true) {
      writer.uint32(40).bool(message.autoResolveIssue);
    }
    if (message.enforceIssueTitle === true) {
      writer.uint32(48).bool(message.enforceIssueTitle);
    }
    if (message.defaultBackupBehavior !== Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED) {
      writer.uint32(56).int32(project_DefaultBackupBehaviorToNumber(message.defaultBackupBehavior));
    }
    return writer;
  },

  decode(input: _m0.Reader | Uint8Array, length?: number): Project {
    const reader = input instanceof _m0.Reader ? input : _m0.Reader.create(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2:
          if (tag !== 18) {
            break;
          }

          message.issueLabels.push(Label.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.forceIssueLabels = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.allowModifyStatement = reader.bool();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.autoResolveIssue = reader.bool();
          continue;
        case 6:
          if (tag !== 48) {
            break;
          }

          message.enforceIssueTitle = reader.bool();
          continue;
        case 7:
          if (tag !== 56) {
            break;
          }

          message.defaultBackupBehavior = project_DefaultBackupBehaviorFromJSON(reader.int32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skipType(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Project {
    return {
      issueLabels: globalThis.Array.isArray(object?.issueLabels)
        ? object.issueLabels.map((e: any) => Label.fromJSON(e))
        : [],
      forceIssueLabels: isSet(object.forceIssueLabels) ? globalThis.Boolean(object.forceIssueLabels) : false,
      allowModifyStatement: isSet(object.allowModifyStatement)
        ? globalThis.Boolean(object.allowModifyStatement)
        : false,
      autoResolveIssue: isSet(object.autoResolveIssue) ? globalThis.Boolean(object.autoResolveIssue) : false,
      enforceIssueTitle: isSet(object.enforceIssueTitle) ? globalThis.Boolean(object.enforceIssueTitle) : false,
      defaultBackupBehavior: isSet(object.defaultBackupBehavior)
        ? project_DefaultBackupBehaviorFromJSON(object.defaultBackupBehavior)
        : Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED,
    };
  },

  toJSON(message: Project): unknown {
    const obj: any = {};
    if (message.issueLabels?.length) {
      obj.issueLabels = message.issueLabels.map((e) => Label.toJSON(e));
    }
    if (message.forceIssueLabels === true) {
      obj.forceIssueLabels = message.forceIssueLabels;
    }
    if (message.allowModifyStatement === true) {
      obj.allowModifyStatement = message.allowModifyStatement;
    }
    if (message.autoResolveIssue === true) {
      obj.autoResolveIssue = message.autoResolveIssue;
    }
    if (message.enforceIssueTitle === true) {
      obj.enforceIssueTitle = message.enforceIssueTitle;
    }
    if (message.defaultBackupBehavior !== Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED) {
      obj.defaultBackupBehavior = project_DefaultBackupBehaviorToJSON(message.defaultBackupBehavior);
    }
    return obj;
  },

  create(base?: DeepPartial<Project>): Project {
    return Project.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Project>): Project {
    const message = createBaseProject();
    message.issueLabels = object.issueLabels?.map((e) => Label.fromPartial(e)) || [];
    message.forceIssueLabels = object.forceIssueLabels ?? false;
    message.allowModifyStatement = object.allowModifyStatement ?? false;
    message.autoResolveIssue = object.autoResolveIssue ?? false;
    message.enforceIssueTitle = object.enforceIssueTitle ?? false;
    message.defaultBackupBehavior = object.defaultBackupBehavior ??
      Project_DefaultBackupBehavior.DEFAULT_BACKUP_BEHAVIOR_UNSPECIFIED;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

if (_m0.util.Long !== Long) {
  _m0.util.Long = Long as any;
  _m0.configure();
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}
