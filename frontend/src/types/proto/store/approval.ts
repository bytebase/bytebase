// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.3.0
//   protoc               unknown
// source: store/approval.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "bytebase.store";

/**
 * IssuePayloadApproval is a part of the payload of an issue.
 * IssuePayloadApproval records the approval template used and the approval history.
 */
export interface IssuePayloadApproval {
  approvalTemplates: ApprovalTemplate[];
  approvers: IssuePayloadApproval_Approver[];
  /**
   * If the value is `false`, it means that the backend is still finding matching approval templates.
   * If `true`, other fields are available.
   */
  approvalFindingDone: boolean;
  approvalFindingError: string;
  riskLevel: IssuePayloadApproval_RiskLevel;
}

export enum IssuePayloadApproval_RiskLevel {
  RISK_LEVEL_UNSPECIFIED = "RISK_LEVEL_UNSPECIFIED",
  LOW = "LOW",
  MODERATE = "MODERATE",
  HIGH = "HIGH",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function issuePayloadApproval_RiskLevelFromJSON(object: any): IssuePayloadApproval_RiskLevel {
  switch (object) {
    case 0:
    case "RISK_LEVEL_UNSPECIFIED":
      return IssuePayloadApproval_RiskLevel.RISK_LEVEL_UNSPECIFIED;
    case 1:
    case "LOW":
      return IssuePayloadApproval_RiskLevel.LOW;
    case 2:
    case "MODERATE":
      return IssuePayloadApproval_RiskLevel.MODERATE;
    case 3:
    case "HIGH":
      return IssuePayloadApproval_RiskLevel.HIGH;
    case -1:
    case "UNRECOGNIZED":
    default:
      return IssuePayloadApproval_RiskLevel.UNRECOGNIZED;
  }
}

export function issuePayloadApproval_RiskLevelToJSON(object: IssuePayloadApproval_RiskLevel): string {
  switch (object) {
    case IssuePayloadApproval_RiskLevel.RISK_LEVEL_UNSPECIFIED:
      return "RISK_LEVEL_UNSPECIFIED";
    case IssuePayloadApproval_RiskLevel.LOW:
      return "LOW";
    case IssuePayloadApproval_RiskLevel.MODERATE:
      return "MODERATE";
    case IssuePayloadApproval_RiskLevel.HIGH:
      return "HIGH";
    case IssuePayloadApproval_RiskLevel.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function issuePayloadApproval_RiskLevelToNumber(object: IssuePayloadApproval_RiskLevel): number {
  switch (object) {
    case IssuePayloadApproval_RiskLevel.RISK_LEVEL_UNSPECIFIED:
      return 0;
    case IssuePayloadApproval_RiskLevel.LOW:
      return 1;
    case IssuePayloadApproval_RiskLevel.MODERATE:
      return 2;
    case IssuePayloadApproval_RiskLevel.HIGH:
      return 3;
    case IssuePayloadApproval_RiskLevel.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface IssuePayloadApproval_Approver {
  /** The new status. */
  status: IssuePayloadApproval_Approver_Status;
  /** The principal id of the approver. */
  principalId: number;
}

export enum IssuePayloadApproval_Approver_Status {
  STATUS_UNSPECIFIED = "STATUS_UNSPECIFIED",
  PENDING = "PENDING",
  APPROVED = "APPROVED",
  REJECTED = "REJECTED",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function issuePayloadApproval_Approver_StatusFromJSON(object: any): IssuePayloadApproval_Approver_Status {
  switch (object) {
    case 0:
    case "STATUS_UNSPECIFIED":
      return IssuePayloadApproval_Approver_Status.STATUS_UNSPECIFIED;
    case 1:
    case "PENDING":
      return IssuePayloadApproval_Approver_Status.PENDING;
    case 2:
    case "APPROVED":
      return IssuePayloadApproval_Approver_Status.APPROVED;
    case 3:
    case "REJECTED":
      return IssuePayloadApproval_Approver_Status.REJECTED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return IssuePayloadApproval_Approver_Status.UNRECOGNIZED;
  }
}

export function issuePayloadApproval_Approver_StatusToJSON(object: IssuePayloadApproval_Approver_Status): string {
  switch (object) {
    case IssuePayloadApproval_Approver_Status.STATUS_UNSPECIFIED:
      return "STATUS_UNSPECIFIED";
    case IssuePayloadApproval_Approver_Status.PENDING:
      return "PENDING";
    case IssuePayloadApproval_Approver_Status.APPROVED:
      return "APPROVED";
    case IssuePayloadApproval_Approver_Status.REJECTED:
      return "REJECTED";
    case IssuePayloadApproval_Approver_Status.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function issuePayloadApproval_Approver_StatusToNumber(object: IssuePayloadApproval_Approver_Status): number {
  switch (object) {
    case IssuePayloadApproval_Approver_Status.STATUS_UNSPECIFIED:
      return 0;
    case IssuePayloadApproval_Approver_Status.PENDING:
      return 1;
    case IssuePayloadApproval_Approver_Status.APPROVED:
      return 2;
    case IssuePayloadApproval_Approver_Status.REJECTED:
      return 3;
    case IssuePayloadApproval_Approver_Status.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface ApprovalTemplate {
  flow: ApprovalFlow | undefined;
  title: string;
  description: string;
  creatorId: number;
}

export interface ApprovalFlow {
  steps: ApprovalStep[];
}

export interface ApprovalStep {
  type: ApprovalStep_Type;
  nodes: ApprovalNode[];
}

/**
 * Type of the ApprovalStep
 * ALL means every node must be approved to proceed.
 * ANY means approving any node will proceed.
 */
export enum ApprovalStep_Type {
  TYPE_UNSPECIFIED = "TYPE_UNSPECIFIED",
  ALL = "ALL",
  ANY = "ANY",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function approvalStep_TypeFromJSON(object: any): ApprovalStep_Type {
  switch (object) {
    case 0:
    case "TYPE_UNSPECIFIED":
      return ApprovalStep_Type.TYPE_UNSPECIFIED;
    case 1:
    case "ALL":
      return ApprovalStep_Type.ALL;
    case 2:
    case "ANY":
      return ApprovalStep_Type.ANY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ApprovalStep_Type.UNRECOGNIZED;
  }
}

export function approvalStep_TypeToJSON(object: ApprovalStep_Type): string {
  switch (object) {
    case ApprovalStep_Type.TYPE_UNSPECIFIED:
      return "TYPE_UNSPECIFIED";
    case ApprovalStep_Type.ALL:
      return "ALL";
    case ApprovalStep_Type.ANY:
      return "ANY";
    case ApprovalStep_Type.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function approvalStep_TypeToNumber(object: ApprovalStep_Type): number {
  switch (object) {
    case ApprovalStep_Type.TYPE_UNSPECIFIED:
      return 0;
    case ApprovalStep_Type.ALL:
      return 1;
    case ApprovalStep_Type.ANY:
      return 2;
    case ApprovalStep_Type.UNRECOGNIZED:
    default:
      return -1;
  }
}

export interface ApprovalNode {
  type: ApprovalNode_Type;
  role: string;
}

/**
 * Type of the ApprovalNode.
 * type determines who should approve this node.
 * ANY_IN_GROUP means the ApprovalNode can be approved by an user from our predefined user group.
 * See GroupValue below for the predefined user groups.
 */
export enum ApprovalNode_Type {
  TYPE_UNSPECIFIED = "TYPE_UNSPECIFIED",
  ANY_IN_GROUP = "ANY_IN_GROUP",
  UNRECOGNIZED = "UNRECOGNIZED",
}

export function approvalNode_TypeFromJSON(object: any): ApprovalNode_Type {
  switch (object) {
    case 0:
    case "TYPE_UNSPECIFIED":
      return ApprovalNode_Type.TYPE_UNSPECIFIED;
    case 1:
    case "ANY_IN_GROUP":
      return ApprovalNode_Type.ANY_IN_GROUP;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ApprovalNode_Type.UNRECOGNIZED;
  }
}

export function approvalNode_TypeToJSON(object: ApprovalNode_Type): string {
  switch (object) {
    case ApprovalNode_Type.TYPE_UNSPECIFIED:
      return "TYPE_UNSPECIFIED";
    case ApprovalNode_Type.ANY_IN_GROUP:
      return "ANY_IN_GROUP";
    case ApprovalNode_Type.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export function approvalNode_TypeToNumber(object: ApprovalNode_Type): number {
  switch (object) {
    case ApprovalNode_Type.TYPE_UNSPECIFIED:
      return 0;
    case ApprovalNode_Type.ANY_IN_GROUP:
      return 1;
    case ApprovalNode_Type.UNRECOGNIZED:
    default:
      return -1;
  }
}

function createBaseIssuePayloadApproval(): IssuePayloadApproval {
  return {
    approvalTemplates: [],
    approvers: [],
    approvalFindingDone: false,
    approvalFindingError: "",
    riskLevel: IssuePayloadApproval_RiskLevel.RISK_LEVEL_UNSPECIFIED,
  };
}

export const IssuePayloadApproval: MessageFns<IssuePayloadApproval> = {
  encode(message: IssuePayloadApproval, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.approvalTemplates) {
      ApprovalTemplate.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.approvers) {
      IssuePayloadApproval_Approver.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.approvalFindingDone !== false) {
      writer.uint32(24).bool(message.approvalFindingDone);
    }
    if (message.approvalFindingError !== "") {
      writer.uint32(34).string(message.approvalFindingError);
    }
    if (message.riskLevel !== IssuePayloadApproval_RiskLevel.RISK_LEVEL_UNSPECIFIED) {
      writer.uint32(40).int32(issuePayloadApproval_RiskLevelToNumber(message.riskLevel));
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IssuePayloadApproval {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIssuePayloadApproval();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.approvalTemplates.push(ApprovalTemplate.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.approvers.push(IssuePayloadApproval_Approver.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.approvalFindingDone = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.approvalFindingError = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.riskLevel = issuePayloadApproval_RiskLevelFromJSON(reader.int32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IssuePayloadApproval {
    return {
      approvalTemplates: globalThis.Array.isArray(object?.approvalTemplates)
        ? object.approvalTemplates.map((e: any) => ApprovalTemplate.fromJSON(e))
        : [],
      approvers: globalThis.Array.isArray(object?.approvers)
        ? object.approvers.map((e: any) => IssuePayloadApproval_Approver.fromJSON(e))
        : [],
      approvalFindingDone: isSet(object.approvalFindingDone) ? globalThis.Boolean(object.approvalFindingDone) : false,
      approvalFindingError: isSet(object.approvalFindingError) ? globalThis.String(object.approvalFindingError) : "",
      riskLevel: isSet(object.riskLevel)
        ? issuePayloadApproval_RiskLevelFromJSON(object.riskLevel)
        : IssuePayloadApproval_RiskLevel.RISK_LEVEL_UNSPECIFIED,
    };
  },

  toJSON(message: IssuePayloadApproval): unknown {
    const obj: any = {};
    if (message.approvalTemplates?.length) {
      obj.approvalTemplates = message.approvalTemplates.map((e) => ApprovalTemplate.toJSON(e));
    }
    if (message.approvers?.length) {
      obj.approvers = message.approvers.map((e) => IssuePayloadApproval_Approver.toJSON(e));
    }
    if (message.approvalFindingDone !== false) {
      obj.approvalFindingDone = message.approvalFindingDone;
    }
    if (message.approvalFindingError !== "") {
      obj.approvalFindingError = message.approvalFindingError;
    }
    if (message.riskLevel !== IssuePayloadApproval_RiskLevel.RISK_LEVEL_UNSPECIFIED) {
      obj.riskLevel = issuePayloadApproval_RiskLevelToJSON(message.riskLevel);
    }
    return obj;
  },

  create(base?: DeepPartial<IssuePayloadApproval>): IssuePayloadApproval {
    return IssuePayloadApproval.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IssuePayloadApproval>): IssuePayloadApproval {
    const message = createBaseIssuePayloadApproval();
    message.approvalTemplates = object.approvalTemplates?.map((e) => ApprovalTemplate.fromPartial(e)) || [];
    message.approvers = object.approvers?.map((e) => IssuePayloadApproval_Approver.fromPartial(e)) || [];
    message.approvalFindingDone = object.approvalFindingDone ?? false;
    message.approvalFindingError = object.approvalFindingError ?? "";
    message.riskLevel = object.riskLevel ?? IssuePayloadApproval_RiskLevel.RISK_LEVEL_UNSPECIFIED;
    return message;
  },
};

function createBaseIssuePayloadApproval_Approver(): IssuePayloadApproval_Approver {
  return { status: IssuePayloadApproval_Approver_Status.STATUS_UNSPECIFIED, principalId: 0 };
}

export const IssuePayloadApproval_Approver: MessageFns<IssuePayloadApproval_Approver> = {
  encode(message: IssuePayloadApproval_Approver, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== IssuePayloadApproval_Approver_Status.STATUS_UNSPECIFIED) {
      writer.uint32(8).int32(issuePayloadApproval_Approver_StatusToNumber(message.status));
    }
    if (message.principalId !== 0) {
      writer.uint32(16).int32(message.principalId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IssuePayloadApproval_Approver {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIssuePayloadApproval_Approver();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = issuePayloadApproval_Approver_StatusFromJSON(reader.int32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.principalId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IssuePayloadApproval_Approver {
    return {
      status: isSet(object.status)
        ? issuePayloadApproval_Approver_StatusFromJSON(object.status)
        : IssuePayloadApproval_Approver_Status.STATUS_UNSPECIFIED,
      principalId: isSet(object.principalId) ? globalThis.Number(object.principalId) : 0,
    };
  },

  toJSON(message: IssuePayloadApproval_Approver): unknown {
    const obj: any = {};
    if (message.status !== IssuePayloadApproval_Approver_Status.STATUS_UNSPECIFIED) {
      obj.status = issuePayloadApproval_Approver_StatusToJSON(message.status);
    }
    if (message.principalId !== 0) {
      obj.principalId = Math.round(message.principalId);
    }
    return obj;
  },

  create(base?: DeepPartial<IssuePayloadApproval_Approver>): IssuePayloadApproval_Approver {
    return IssuePayloadApproval_Approver.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IssuePayloadApproval_Approver>): IssuePayloadApproval_Approver {
    const message = createBaseIssuePayloadApproval_Approver();
    message.status = object.status ?? IssuePayloadApproval_Approver_Status.STATUS_UNSPECIFIED;
    message.principalId = object.principalId ?? 0;
    return message;
  },
};

function createBaseApprovalTemplate(): ApprovalTemplate {
  return { flow: undefined, title: "", description: "", creatorId: 0 };
}

export const ApprovalTemplate: MessageFns<ApprovalTemplate> = {
  encode(message: ApprovalTemplate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.flow !== undefined) {
      ApprovalFlow.encode(message.flow, writer.uint32(10).fork()).join();
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.creatorId !== 0) {
      writer.uint32(32).int32(message.creatorId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApprovalTemplate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApprovalTemplate();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.flow = ApprovalFlow.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.creatorId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApprovalTemplate {
    return {
      flow: isSet(object.flow) ? ApprovalFlow.fromJSON(object.flow) : undefined,
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      creatorId: isSet(object.creatorId) ? globalThis.Number(object.creatorId) : 0,
    };
  },

  toJSON(message: ApprovalTemplate): unknown {
    const obj: any = {};
    if (message.flow !== undefined) {
      obj.flow = ApprovalFlow.toJSON(message.flow);
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.creatorId !== 0) {
      obj.creatorId = Math.round(message.creatorId);
    }
    return obj;
  },

  create(base?: DeepPartial<ApprovalTemplate>): ApprovalTemplate {
    return ApprovalTemplate.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApprovalTemplate>): ApprovalTemplate {
    const message = createBaseApprovalTemplate();
    message.flow = (object.flow !== undefined && object.flow !== null)
      ? ApprovalFlow.fromPartial(object.flow)
      : undefined;
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.creatorId = object.creatorId ?? 0;
    return message;
  },
};

function createBaseApprovalFlow(): ApprovalFlow {
  return { steps: [] };
}

export const ApprovalFlow: MessageFns<ApprovalFlow> = {
  encode(message: ApprovalFlow, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.steps) {
      ApprovalStep.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApprovalFlow {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApprovalFlow();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.steps.push(ApprovalStep.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApprovalFlow {
    return {
      steps: globalThis.Array.isArray(object?.steps) ? object.steps.map((e: any) => ApprovalStep.fromJSON(e)) : [],
    };
  },

  toJSON(message: ApprovalFlow): unknown {
    const obj: any = {};
    if (message.steps?.length) {
      obj.steps = message.steps.map((e) => ApprovalStep.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ApprovalFlow>): ApprovalFlow {
    return ApprovalFlow.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApprovalFlow>): ApprovalFlow {
    const message = createBaseApprovalFlow();
    message.steps = object.steps?.map((e) => ApprovalStep.fromPartial(e)) || [];
    return message;
  },
};

function createBaseApprovalStep(): ApprovalStep {
  return { type: ApprovalStep_Type.TYPE_UNSPECIFIED, nodes: [] };
}

export const ApprovalStep: MessageFns<ApprovalStep> = {
  encode(message: ApprovalStep, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== ApprovalStep_Type.TYPE_UNSPECIFIED) {
      writer.uint32(8).int32(approvalStep_TypeToNumber(message.type));
    }
    for (const v of message.nodes) {
      ApprovalNode.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApprovalStep {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApprovalStep();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = approvalStep_TypeFromJSON(reader.int32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.nodes.push(ApprovalNode.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApprovalStep {
    return {
      type: isSet(object.type) ? approvalStep_TypeFromJSON(object.type) : ApprovalStep_Type.TYPE_UNSPECIFIED,
      nodes: globalThis.Array.isArray(object?.nodes) ? object.nodes.map((e: any) => ApprovalNode.fromJSON(e)) : [],
    };
  },

  toJSON(message: ApprovalStep): unknown {
    const obj: any = {};
    if (message.type !== ApprovalStep_Type.TYPE_UNSPECIFIED) {
      obj.type = approvalStep_TypeToJSON(message.type);
    }
    if (message.nodes?.length) {
      obj.nodes = message.nodes.map((e) => ApprovalNode.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ApprovalStep>): ApprovalStep {
    return ApprovalStep.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApprovalStep>): ApprovalStep {
    const message = createBaseApprovalStep();
    message.type = object.type ?? ApprovalStep_Type.TYPE_UNSPECIFIED;
    message.nodes = object.nodes?.map((e) => ApprovalNode.fromPartial(e)) || [];
    return message;
  },
};

function createBaseApprovalNode(): ApprovalNode {
  return { type: ApprovalNode_Type.TYPE_UNSPECIFIED, role: "" };
}

export const ApprovalNode: MessageFns<ApprovalNode> = {
  encode(message: ApprovalNode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== ApprovalNode_Type.TYPE_UNSPECIFIED) {
      writer.uint32(8).int32(approvalNode_TypeToNumber(message.type));
    }
    if (message.role !== "") {
      writer.uint32(18).string(message.role);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApprovalNode {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseApprovalNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.type = approvalNode_TypeFromJSON(reader.int32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.role = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ApprovalNode {
    return {
      type: isSet(object.type) ? approvalNode_TypeFromJSON(object.type) : ApprovalNode_Type.TYPE_UNSPECIFIED,
      role: isSet(object.role) ? globalThis.String(object.role) : "",
    };
  },

  toJSON(message: ApprovalNode): unknown {
    const obj: any = {};
    if (message.type !== ApprovalNode_Type.TYPE_UNSPECIFIED) {
      obj.type = approvalNode_TypeToJSON(message.type);
    }
    if (message.role !== "") {
      obj.role = message.role;
    }
    return obj;
  },

  create(base?: DeepPartial<ApprovalNode>): ApprovalNode {
    return ApprovalNode.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ApprovalNode>): ApprovalNode {
    const message = createBaseApprovalNode();
    message.type = object.type ?? ApprovalNode_Type.TYPE_UNSPECIFIED;
    message.role = object.role ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
