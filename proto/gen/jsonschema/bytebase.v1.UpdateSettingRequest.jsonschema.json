{
  "$id": "bytebase.v1.UpdateSettingRequest.jsonschema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "The request message for updating or creating a setting.",
  "patternProperties": {
    "^(allow_missing)$": {
      "default": false,
      "type": "boolean"
    },
    "^(update_mask)$": {
      "$ref": "google.protobuf.FieldMask.jsonschema.json"
    },
    "^(validate_only)$": {
      "default": false,
      "description": "validate_only is a flag to indicate whether to validate the setting value,\n server would not persist the setting value if it is true.",
      "type": "boolean"
    }
  },
  "properties": {
    "allowMissing": {
      "default": false,
      "type": "boolean"
    },
    "setting": {
      "$ref": "bytebase.v1.Setting.jsonschema.json",
      "description": "The setting to update."
    },
    "updateMask": {
      "$ref": "google.protobuf.FieldMask.jsonschema.json"
    },
    "validateOnly": {
      "default": false,
      "description": "validate_only is a flag to indicate whether to validate the setting value,\n server would not persist the setting value if it is true.",
      "type": "boolean"
    }
  },
  "title": "Update Setting Request",
  "type": "object"
}
