{
  "$id": "bytebase.store.DatabaseSchemaMetadata.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "DatabaseSchemaMetadata is the schema metadata for databases.",
  "properties": {
    "characterSet": {
      "description": "The character set of the database.",
      "type": "string"
    },
    "collation": {
      "description": "The collation of the database.",
      "type": "string"
    },
    "datashare": {
      "description": "The database belongs to a datashare.",
      "type": "boolean"
    },
    "eventTriggers": {
      "description": "The list of event triggers in a database (PostgreSQL specific).\n Event triggers are database-level objects, not schema-scoped.",
      "items": {
        "$ref": "bytebase.store.EventTriggerMetadata.jsonschema.strict.json"
      },
      "type": "array"
    },
    "extensions": {
      "description": "The list of extensions in a database.",
      "items": {
        "$ref": "bytebase.store.ExtensionMetadata.jsonschema.strict.json"
      },
      "type": "array"
    },
    "linkedDatabases": {
      "items": {
        "$ref": "bytebase.store.LinkedDatabaseMetadata.jsonschema.strict.json"
      },
      "type": "array"
    },
    "name": {
      "type": "string"
    },
    "owner": {
      "type": "string"
    },
    "schemas": {
      "description": "The list of schemas in a database.",
      "items": {
        "$ref": "bytebase.store.SchemaMetadata.jsonschema.strict.json"
      },
      "type": "array"
    },
    "searchPath": {
      "description": "The search_path is the search path of a PostgreSQL database.",
      "type": "string"
    },
    "serviceName": {
      "description": "The service name of the database. It's an Oracle-specific concept.",
      "type": "string"
    }
  },
  "required": [
    "name",
    "characterSet",
    "collation",
    "datashare",
    "serviceName",
    "owner",
    "searchPath"
  ],
  "title": "Database Schema Metadata",
  "type": "object"
}
