{
  "$id": "bytebase.v1.AdminExecuteRequest.jsonschema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "properties": {
    "container": {
      "description": "Container is the container name to execute the query against, used for\n CosmosDB only.",
      "type": "string"
    },
    "limit": {
      "description": "The maximum number of rows to return.",
      "maximum": 2147483647,
      "minimum": -2147483648,
      "type": "integer"
    },
    "name": {
      "description": "The name is the instance name to execute the query against.\n Format: instances/{instance}/databases/{databaseName}",
      "type": "string"
    },
    "schema": {
      "description": "The default schema to execute the statement. Equals to the current schema\n in Oracle and search path in Postgres.",
      "type": "string"
    },
    "statement": {
      "description": "The SQL statement to execute.",
      "type": "string"
    }
  },
  "required": [
    "name",
    "statement",
    "limit"
  ],
  "title": "Admin Execute Request",
  "type": "object"
}
