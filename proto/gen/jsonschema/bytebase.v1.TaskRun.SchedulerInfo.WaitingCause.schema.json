{
  "$id": "bytebase.v1.TaskRun.SchedulerInfo.WaitingCause.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Information about why a task run is waiting.",
  "patternProperties": {
    "^(connectionLimit)$": {
      "description": "Waiting due to connection limit reached.",
      "type": "boolean"
    },
    "^(parallelTasksLimit)$": {
      "description": "Waiting due to parallel tasks limit.",
      "type": "boolean"
    }
  },
  "properties": {
    "connection_limit": {
      "description": "Waiting due to connection limit reached.",
      "type": "boolean"
    },
    "parallel_tasks_limit": {
      "description": "Waiting due to parallel tasks limit.",
      "type": "boolean"
    },
    "task": {
      "$ref": "bytebase.v1.TaskRun.SchedulerInfo.WaitingCause.Task.schema.json",
      "description": "Waiting for another task to complete."
    }
  },
  "title": "Waiting Cause",
  "type": "object"
}
