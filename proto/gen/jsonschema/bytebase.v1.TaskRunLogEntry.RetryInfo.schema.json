{
  "$id": "bytebase.v1.TaskRunLogEntry.RetryInfo.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Retry information for failed operations.",
  "patternProperties": {
    "^(maximumRetries)$": {
      "anyOf": [
        {
          "maximum": 2147483647,
          "minimum": -2147483648,
          "type": "integer"
        },
        {
          "pattern": "^-?[0-9]+$",
          "type": "string"
        }
      ],
      "default": 0,
      "description": "Maximum number of retries allowed."
    },
    "^(retryCount)$": {
      "anyOf": [
        {
          "maximum": 2147483647,
          "minimum": -2147483648,
          "type": "integer"
        },
        {
          "pattern": "^-?[0-9]+$",
          "type": "string"
        }
      ],
      "default": 0,
      "description": "Current retry attempt number."
    }
  },
  "properties": {
    "error": {
      "default": "",
      "description": "The error that triggered the retry.",
      "type": "string"
    },
    "maximum_retries": {
      "anyOf": [
        {
          "maximum": 2147483647,
          "minimum": -2147483648,
          "type": "integer"
        },
        {
          "pattern": "^-?[0-9]+$",
          "type": "string"
        }
      ],
      "default": 0,
      "description": "Maximum number of retries allowed."
    },
    "retry_count": {
      "anyOf": [
        {
          "maximum": 2147483647,
          "minimum": -2147483648,
          "type": "integer"
        },
        {
          "pattern": "^-?[0-9]+$",
          "type": "string"
        }
      ],
      "default": 0,
      "description": "Current retry attempt number."
    }
  },
  "title": "Retry Info",
  "type": "object"
}
