{
  "$id": "bytebase.store.SchemaMetadata.schema.strict.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "SchemaMetadata is the metadata for schemas.\n This is the concept of schema in Postgres, but it's a no-op for MySQL.",
  "properties": {
    "comment": {
      "type": "string"
    },
    "enum_types": {
      "items": {
        "$ref": "bytebase.store.EnumTypeMetadata.schema.strict.json"
      },
      "type": "array"
    },
    "events": {
      "items": {
        "$ref": "bytebase.store.EventMetadata.schema.strict.json"
      },
      "type": "array"
    },
    "external_tables": {
      "description": "The list of external tables in a schema.",
      "items": {
        "$ref": "bytebase.store.ExternalTableMetadata.schema.strict.json"
      },
      "type": "array"
    },
    "functions": {
      "description": "The list of functions in a schema.",
      "items": {
        "$ref": "bytebase.store.FunctionMetadata.schema.strict.json"
      },
      "type": "array"
    },
    "materialized_views": {
      "description": "The list of materialized views in a schema.",
      "items": {
        "$ref": "bytebase.store.MaterializedViewMetadata.schema.strict.json"
      },
      "type": "array"
    },
    "name": {
      "description": "The schema name.\n It is an empty string for databases without such concept such as MySQL.",
      "type": "string"
    },
    "owner": {
      "type": "string"
    },
    "packages": {
      "description": "The list of packages in a schema.",
      "items": {
        "$ref": "bytebase.store.PackageMetadata.schema.strict.json"
      },
      "type": "array"
    },
    "procedures": {
      "description": "The list of procedures in a schema.",
      "items": {
        "$ref": "bytebase.store.ProcedureMetadata.schema.strict.json"
      },
      "type": "array"
    },
    "sequences": {
      "description": "The list of sequences in a schema.",
      "items": {
        "$ref": "bytebase.store.SequenceMetadata.schema.strict.json"
      },
      "type": "array"
    },
    "skip_dump": {
      "type": "boolean"
    },
    "streams": {
      "description": "The list of streams in a schema, currently only used for Snowflake.",
      "items": {
        "$ref": "bytebase.store.StreamMetadata.schema.strict.json"
      },
      "type": "array"
    },
    "tables": {
      "description": "The list of tables in a schema.",
      "items": {
        "$ref": "bytebase.store.TableMetadata.schema.strict.json"
      },
      "type": "array"
    },
    "tasks": {
      "description": "The list of tasks in a schema, currently only used for Snowflake.",
      "items": {
        "$ref": "bytebase.store.TaskMetadata.schema.strict.json"
      },
      "type": "array"
    },
    "views": {
      "description": "The list of views in a schema.",
      "items": {
        "$ref": "bytebase.store.ViewMetadata.schema.strict.json"
      },
      "type": "array"
    }
  },
  "required": [
    "name",
    "owner",
    "comment",
    "skip_dump"
  ],
  "title": "Schema Metadata",
  "type": "object"
}
