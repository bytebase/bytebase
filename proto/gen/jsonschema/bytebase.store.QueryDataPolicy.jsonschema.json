{
  "$id": "bytebase.store.QueryDataPolicy.jsonschema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "QueryDataPolicy is the policy configuration for querying data.",
  "patternProperties": {
    "^(disable_copy_data)$": {
      "default": false,
      "description": "Disable copying data.",
      "type": "boolean"
    },
    "^(disable_export)$": {
      "default": false,
      "description": "Disable exporting data in the SQL editor.",
      "type": "boolean"
    },
    "^(maximum_result_rows)$": {
      "anyOf": [
        {
          "maximum": 2147483647,
          "minimum": -2147483648,
          "type": "integer"
        },
        {
          "pattern": "^-?[0-9]+$",
          "type": "string"
        }
      ],
      "default": 0,
      "description": "The return rows limit.\n The default value is -1, means no limit."
    },
    "^(maximum_result_size)$": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "pattern": "^-?[0-9]+$",
          "type": "string"
        }
      ],
      "default": 0,
      "description": "The size limit in bytes.\n The default value is 100MB, we will use the default value if the setting not exists, or the limit \u003c= 0."
    }
  },
  "properties": {
    "disableCopyData": {
      "default": false,
      "description": "Disable copying data.",
      "type": "boolean"
    },
    "disableExport": {
      "default": false,
      "description": "Disable exporting data in the SQL editor.",
      "type": "boolean"
    },
    "maximumResultRows": {
      "anyOf": [
        {
          "maximum": 2147483647,
          "minimum": -2147483648,
          "type": "integer"
        },
        {
          "pattern": "^-?[0-9]+$",
          "type": "string"
        }
      ],
      "default": 0,
      "description": "The return rows limit.\n The default value is -1, means no limit."
    },
    "maximumResultSize": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "pattern": "^-?[0-9]+$",
          "type": "string"
        }
      ],
      "default": 0,
      "description": "The size limit in bytes.\n The default value is 100MB, we will use the default value if the setting not exists, or the limit \u003c= 0."
    },
    "timeout": {
      "$ref": "google.protobuf.Duration.jsonschema.json",
      "description": "The query timeout duration."
    }
  },
  "title": "Query Data Policy",
  "type": "object"
}
