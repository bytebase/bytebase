{
  "$id": "bytebase.v1.BatchCancelTaskRunsRequest.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "patternProperties": {
    "^(taskRuns)$": {
      "description": "The taskRuns to cancel.\n Format: projects/{project}/rollouts/{rollout}/stages/{stage}/tasks/{task}/taskRuns/{taskRun}",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "properties": {
    "parent": {
      "default": "",
      "description": "The name of the parent of the taskRuns.\n Format: projects/{project}/rollouts/{rollout}/stages/{stage}/tasks/{task}\n Use `projects/{project}/rollouts/{rollout}/stages/{stage}/tasks/-` to cancel task runs under the same stage.",
      "type": "string"
    },
    "reason": {
      "default": "",
      "description": "The reason for canceling the task runs.",
      "maxLength": 1000,
      "type": "string"
    },
    "task_runs": {
      "description": "The taskRuns to cancel.\n Format: projects/{project}/rollouts/{rollout}/stages/{stage}/tasks/{task}/taskRuns/{taskRun}",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "title": "Batch Cancel Task Runs Request",
  "type": "object"
}
