{
  "$id": "bytebase.v1.UpdateSettingRequest.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "The request message for updating or creating a setting.",
  "patternProperties": {
    "^(allowMissing)$": {
      "default": false,
      "type": "boolean"
    },
    "^(updateMask)$": {
      "$ref": "google.protobuf.FieldMask.schema.json"
    },
    "^(validateOnly)$": {
      "default": false,
      "description": "validate_only is a flag to indicate whether to validate the setting value,\n server would not persist the setting value if it is true.",
      "type": "boolean"
    }
  },
  "properties": {
    "allow_missing": {
      "default": false,
      "type": "boolean"
    },
    "setting": {
      "$ref": "bytebase.v1.Setting.schema.json",
      "description": "The setting to update."
    },
    "update_mask": {
      "$ref": "google.protobuf.FieldMask.schema.json"
    },
    "validate_only": {
      "default": false,
      "description": "validate_only is a flag to indicate whether to validate the setting value,\n server would not persist the setting value if it is true.",
      "type": "boolean"
    }
  },
  "title": "Update Setting Request",
  "type": "object"
}
