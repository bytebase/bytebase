{
  "$id": "bytebase.v1.TaskRunLogEntry.CommandExecute.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Command execution details.",
  "patternProperties": {
    "^(commandIndexes)$": {
      "description": "Either `command_indexes` or `statement` is set.\n The indexes of the executed commands.",
      "items": {
        "anyOf": [
          {
            "maximum": 2147483647,
            "minimum": -2147483648,
            "type": "integer"
          },
          {
            "pattern": "^-?[0-9]+$",
            "type": "string"
          }
        ]
      },
      "type": "array"
    },
    "^(logTime)$": {
      "$ref": "google.protobuf.Timestamp.schema.json",
      "description": "When the command was logged."
    }
  },
  "properties": {
    "command_indexes": {
      "description": "Either `command_indexes` or `statement` is set.\n The indexes of the executed commands.",
      "items": {
        "anyOf": [
          {
            "maximum": 2147483647,
            "minimum": -2147483648,
            "type": "integer"
          },
          {
            "pattern": "^-?[0-9]+$",
            "type": "string"
          }
        ]
      },
      "type": "array"
    },
    "log_time": {
      "$ref": "google.protobuf.Timestamp.schema.json",
      "description": "When the command was logged."
    },
    "response": {
      "$ref": "bytebase.v1.TaskRunLogEntry.CommandExecute.CommandResponse.schema.json",
      "description": "The response from executing the command."
    },
    "statement": {
      "default": "",
      "description": "The executed statement.",
      "type": "string"
    }
  },
  "title": "Command Execute",
  "type": "object"
}
