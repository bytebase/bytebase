{
  "$id": "bytebase.v1.TaskRunSession.Postgres.schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "PostgreSQL session information.",
  "patternProperties": {
    "^(blockedSessions)$": {
      "description": "`blocked_sessions` are blocked by `session`.",
      "items": {
        "$ref": "bytebase.v1.TaskRunSession.Postgres.Session.schema.json"
      },
      "type": "array"
    },
    "^(blockingSessions)$": {
      "description": "`blocking_sessions` block `session`.",
      "items": {
        "$ref": "bytebase.v1.TaskRunSession.Postgres.Session.schema.json"
      },
      "type": "array"
    }
  },
  "properties": {
    "blocked_sessions": {
      "description": "`blocked_sessions` are blocked by `session`.",
      "items": {
        "$ref": "bytebase.v1.TaskRunSession.Postgres.Session.schema.json"
      },
      "type": "array"
    },
    "blocking_sessions": {
      "description": "`blocking_sessions` block `session`.",
      "items": {
        "$ref": "bytebase.v1.TaskRunSession.Postgres.Session.schema.json"
      },
      "type": "array"
    },
    "session": {
      "$ref": "bytebase.v1.TaskRunSession.Postgres.Session.schema.json",
      "description": "`session` is the session of the task run executing commands."
    }
  },
  "title": "Postgres",
  "type": "object"
}
