This directory contains historical design and implementation records.

- Preserve the paths, framework names, and migration terminology that were accurate when each record was written.
- Do not use those historical paths as current frontend architecture guidance.
- For current frontend placement and dependency rules, follow `../../frontend/AGENTS.md` and verify every referenced path in the working tree.
- New implementation plans must use the current route-first ownership map from `frontend/AGENTS.md`.
