# Historical plans

Files in this directory are implementation snapshots from the date in each filename. Paths, package names, commands, and APIs in them are historical context, not current development instructions. Use the current source tree, `AGENTS.md`, and `docs/agents/` for active guidance.
