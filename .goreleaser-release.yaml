builds:
  - skip: true
changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
release:
  draft: true
  mode: append
  extra_files:
    - glob: ../artifacts/checksums.txt
    - glob: ../artifacts/*/bytebase*tar.gz
  name_template: "Release {{.Tag}}"
  header: |
    # What's Changed

    ## 🚀 Features

    ## 🎄 Enhancements

    ## 🎠 Community

    ## New Contributors

    ## Code Diff
