package mysql

// Framework code is generated by the generator.

import (
	"testing"

	"github.com/bytebase/bytebase/plugin/advisor"
)

func TestInsertUpdateNoOrderBy(t *testing.T) {
	tests := []advisor.TestCase{
		{
			Statement: `INSERT INTO tech_book values(1, 'a')`,
			Want: []advisor.Advice{
				{
					Status:  advisor.Success,
					Code:    advisor.Ok,
					Title:   "OK",
					Content: "",
				},
			},
		},
		{
			Statement: `INSERT INTO tech_book SELECT * FROM tech_book ORDER BY id`,
			Want: []advisor.Advice{
				{
					Status:  advisor.Warn,
					Code:    advisor.InsertUseOrderBy,
					Title:   "insert-update.no-order-by",
					Content: "ORDER BY clause is forbidden in INSERT and UPDATE statement, but \"INSERT INTO tech_book SELECT * FROM tech_book ORDER BY id\" uses",
					Line:    1,
				},
			},
		},
		{
			Statement: `INSERT INTO tech_book SELECT * FROM tech_book UNION SELECT * FROM tech_book ORDER BY id`,
			Want: []advisor.Advice{
				{
					Status:  advisor.Warn,
					Code:    advisor.InsertUseOrderBy,
					Title:   "insert-update.no-order-by",
					Content: "ORDER BY clause is forbidden in INSERT and UPDATE statement, but \"INSERT INTO tech_book SELECT * FROM tech_book UNION SELECT * FROM tech_book ORDER BY id\" uses",
					Line:    1,
				},
			},
		},
		{
			Statement: `UPDATE tech_book SET name = 'my name' ORDER BY id`,
			Want: []advisor.Advice{
				{
					Status:  advisor.Warn,
					Code:    advisor.UpdateUseOrderBy,
					Title:   "insert-update.no-order-by",
					Content: "ORDER BY clause is forbidden in INSERT and UPDATE statement, but \"UPDATE tech_book SET name = 'my name' ORDER BY id\" uses",
					Line:    1,
				},
			},
		},
	}

	advisor.RunSQLReviewRuleTests(t, tests, &InsertUpdateNoOrderByAdvisor{}, &advisor.SQLReviewRule{
		Type:    advisor.SchemaRuleInsertUpdateNoOrderBy,
		Level:   advisor.SchemaRuleLevelWarning,
		Payload: "",
	}, advisor.MockMySQLDatabase)
}
