package ast

// UnconvertedExpressionDef is the struct for unconveted expression definition.
// TODO(rebelice): remove it.
// We define this because we can not convert all expression types now.
type UnconvertedExpressionDef struct {
	expression
}
