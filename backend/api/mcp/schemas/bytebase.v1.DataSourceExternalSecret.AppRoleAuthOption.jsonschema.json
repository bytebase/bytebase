{
  "$id": "bytebase.v1.DataSourceExternalSecret.AppRoleAuthOption.jsonschema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "patternProperties": {
    "^(mount_path)$": {
      "default": "",
      "description": "The path where the approle auth method is mounted.",
      "type": "string"
    },
    "^(role_id)$": {
      "default": "",
      "description": "The role ID for Vault AppRole authentication.",
      "type": "string"
    },
    "^(secret_id)$": {
      "default": "",
      "description": "the secret id for the role without ttl.",
      "type": "string"
    }
  },
  "properties": {
    "mountPath": {
      "default": "",
      "description": "The path where the approle auth method is mounted.",
      "type": "string"
    },
    "roleId": {
      "default": "",
      "description": "The role ID for Vault AppRole authentication.",
      "type": "string"
    },
    "secretId": {
      "default": "",
      "description": "the secret id for the role without ttl.",
      "type": "string"
    },
    "type": {
      "anyOf": [
        {
          "pattern": "^SECRET_TYPE_UNSPECIFIED$",
          "type": "string"
        },
        {
          "enum": [
            "PLAIN",
            "ENVIRONMENT"
          ],
          "type": "string"
        },
        {
          "maximum": 2147483647,
          "minimum": -2147483648,
          "type": "integer"
        }
      ],
      "default": 0,
      "description": "The type of secret for AppRole authentication.",
      "title": "Secret Type"
    }
  },
  "title": "App Role Auth Option",
  "type": "object"
}
