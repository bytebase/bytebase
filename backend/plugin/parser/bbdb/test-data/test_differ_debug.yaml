- oldSchema: |
    CREATE TABLE `t1` (`a` int);
  newSchema: |
    CREATE TABLE `t1` (
      `a` int,
      `b` char(20),
      `c` int
    );
  diff: |+
    ALTER TABLE `t1` ADD COLUMN IF NOT EXISTS `b` char(20) DEFAULT NULL AFTER `a`;

    ALTER TABLE `t1` ADD COLUMN IF NOT EXISTS `c` int DEFAULT NULL AFTER `b`;

