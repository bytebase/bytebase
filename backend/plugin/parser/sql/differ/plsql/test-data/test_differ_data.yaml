- oldSchema: |-
    CREATE TABLE "TEST"."TEST_TABLE" ( 
      "ID" NUMBER(10,0) NOT NULL ENABLE, 
      "NAME" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
      "DESCRIPTION" VARCHAR2(100 BYTE), 
      "UPDATED" DATE DEFAULT SYSDATE NOT NULL ENABLE, 
      "DELETED" DATE, 
      CONSTRAINT "TEST_TABLE_UK" UNIQUE ("NAME") ENABLE,
      CONSTRAINT "TEST_TABLE_UK2" UNIQUE ("DESCRIPTION") ENABLE,
      CONSTRAINT "TEST_TABLE_FK" FOREIGN KEY ("ID") REFERENCES "TEST"."TEST_TABLE_B" ("ID") ENABLE
    );
    CREATE TABLE "TEST"."TEST_TABLE_B" (
      "ID" NUMBER(10,0) NOT NULL ENABLE, 
      "NAME" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
      "DESCRIPTION" VARCHAR2(100 BYTE),
      CONSTRAINT "TEST_TABLE_B_PK" PRIMARY KEY ("ID") ENABLE
    );
  newSchema: |-
    CREATE TABLE "TEST"."TEST_TABLE" ( 
      "ID" NUMBER(10,0) NOT NULL ENABLE, 
      "NAME" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
      "DESCRIPTION" VARCHAR2(200 BYTE), 
      "CREATED" DATE DEFAULT SYSDATE NOT NULL ENABLE, 
      "UPDATED" DATE DEFAULT SYSDATE NOT NULL ENABLE, 
      "DELETED" DATE, 
      CONSTRAINT "TEST_TABLE_PK" PRIMARY KEY ("ID") ENABLE,
      CONSTRAINT "TEST_TABLE_UK2" UNIQUE ("DESCRIPTION") ENABLE,
      CONSTRAINT "TEST_TABLE_FK" FOREIGN KEY ("ID") REFERENCES "TEST"."TEST_TABLE_C" ("ID") ENABLE
    );
    CREATE TABLE "TEST"."TEST_TABLE_C" (
      "ID" NUMBER(10,0) NOT NULL ENABLE, 
      "NAME" VARCHAR2(100 BYTE) NOT NULL ENABLE, 
      "DESCRIPTION" VARCHAR2(100 BYTE),
      CONSTRAINT "TEST_TABLE_C_PK" PRIMARY KEY ("ID") ENABLE
    );
  diff: ""
