distribution: local
vectorized: true

• upsert
│ into: t2(id, c, d, grp, v)
│ auto commit
│
└── • index join
    │ estimated row count: 100
    │ table: t@t_pkey
    │
    └── • scan
          estimated row count: 100 (10% of the table; stats collected 40 seconds ago)
          table: t@idx_c
          spans: [ - /9]

index recommendations: 1
1. type: index replacement
   SQL commands: CREATE INDEX ON capture.public.t (c) STORING (d, grp, v) /* After successfully creating the replacement index, manually run: `ALTER INDEX capture.public.t@idx_c NOT VISIBLE` and then, after verifying workload performance, manually run: `DROP INDEX capture.public.t@idx_c` */;
