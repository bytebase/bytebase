package pg

// Framework code is generated by the generator.

import (
	"github.com/pkg/errors"

	"github.com/bytebase/bytebase/backend/plugin/advisor"
	"github.com/bytebase/bytebase/backend/plugin/parser/sql/ast"
	storepb "github.com/bytebase/bytebase/proto/generated-go/store"
)

var (
	_ advisor.Advisor = (*StatementDisallowMixDmlAdvisor)(nil)
	_ ast.Visitor     = (*statementDisallowMixDmlChecker)(nil)
)

func init() {
	advisor.Register(storepb.Engine_POSTGRES, advisor.PostgreSQLStatementDisallowMixDML, &StatementDisallowMixDmlAdvisor{})
}

// StatementDisallowMixDmlAdvisor is the advisor checking for disallow mix DML on same table.
type StatementDisallowMixDmlAdvisor struct {
}

// Check checks for disallow mix DML on same table.
func (*StatementDisallowMixDmlAdvisor) Check(ctx advisor.Context, _ string) ([]advisor.Advice, error) {
	// TODO(p0ny): implement it.
	stmtList, ok := ctx.AST.([]ast.Node)
	if !ok {
		return nil, errors.Errorf("failed to convert to Node")
	}

	level, err := advisor.NewStatusBySQLReviewRuleLevel(ctx.Rule.Level)
	if err != nil {
		return nil, err
	}
	checker := &statementDisallowMixDmlChecker{
		level: level,
		title: string(ctx.Rule.Type),
	}

	for _, stmt := range stmtList {
		ast.Walk(checker, stmt)
	}

	if len(checker.adviceList) == 0 {
		checker.adviceList = append(checker.adviceList, advisor.Advice{
			Status:  advisor.Success,
			Code:    advisor.Ok,
			Title:   "OK",
			Content: "",
		})
	}
	return checker.adviceList, nil
}

type statementDisallowMixDmlChecker struct {
	adviceList []advisor.Advice
	level      advisor.Status
	title      string
}

// Visit implements ast.Visitor interface.
func (checker *statementDisallowMixDmlChecker) Visit(_ ast.Node) ast.Visitor {
	// TODO: implement it
	// switch node := in.(type) {
	// }

	return checker
}
