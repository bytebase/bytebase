name: Backend Tests

on:
  pull_request:
    branches:
      - main
      - "release/*.*.*"
    paths-ignore:
      - "backend/**"
      - "go.mod"

jobs:
  go-tests:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Not required"'
