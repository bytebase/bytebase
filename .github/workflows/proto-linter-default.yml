name: Proto linter

on:
  pull_request:
    branches:
      - 'main'
      - 'release/*.*.*'
    paths-ignore:
      - 'proto/**'

jobs:
  lint-protos:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "Not required"'
